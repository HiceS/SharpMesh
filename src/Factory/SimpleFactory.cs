namespace SharpMesh.Factory
{
    /// <summary>
    /// Will generate a simple Mesh based off a factory pattern.
    /// TODO: Generate a factory base object that takes in a set of parameters as options.
    /// </summary>
    public class SimpleFactory
    {
        
    }
}